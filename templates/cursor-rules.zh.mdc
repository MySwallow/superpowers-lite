---
description: superpowers-lite 方法论 skill 集 —— 调试/计划/头脑风暴/并行子代理（始终加载）
alwaysApply: true
---

# superpowers-lite （Cursor / Windsurf / Cline 适配版）

> Cursor 没有 skill 概念，只有 rules。本文件是把上游 `using-superpowers` skill
> 的核心规则 inline 进 rule，剩下 6 个 skill 通过 Read 工具按需加载。

## 核心规则

在回应用户消息之前，**先**检查下面"6 个 skill"是否有适用的。
如果有（哪怕只有 1% 可能），先用 Read 工具读取对应的 `SKILL.md`，
**再**产生任何其他输出（包括澄清问题）。
简短声明："Using [skill] to [purpose]"。

## 6 个可用 skill（按需 Read）

skill 装在 `<SKILLS_PATH>`（已展开为绝对路径）：

| 触发场景 | Skill | 文件 |
|---|---|---|
| 任何 bug、测试失败、异常行为 | systematic-debugging | `<SKILLS_PATH>/systematic-debugging/SKILL.md` |
| 任何创造性工作前（新功能 / 组件 / 能力） | brainstorming | `<SKILLS_PATH>/brainstorming/SKILL.md` |
| 把规格拆成可执行的多步骤计划 | writing-plans | `<SKILLS_PATH>/writing-plans/SKILL.md` |
| 在当前会话执行已有计划 | executing-plans | `<SKILLS_PATH>/executing-plans/SKILL.md` |
| 用全新子代理执行计划 + 两阶段审核 | subagent-driven-development | `<SKILLS_PATH>/subagent-driven-development/SKILL.md` |
| 2+ 个彼此独立的任务，并行处理 | dispatching-parallel-agents | `<SKILLS_PATH>/dispatching-parallel-agents/SKILL.md` |

读到 `SKILL.md` 之后：

1. 如果 skill 中有检查清单 → 为每项创建对应的 todo 跟踪
2. 严格遵循 skill 步骤，不要凭印象简化

## 指令优先级（最高 → 最低）

1. **用户指令** —— CLAUDE.md / AGENTS.md / 用户直接请求
2. **本 rule + skills** —— 冲突时覆盖默认系统行为
3. **默认系统 prompt**

用户与 skill 冲突时，遵循用户。用户说了算。

## 红色信号 —— 如果你想到以下任一，停下来检查 skill

- "这只是一个简单问题" → 问题就是任务，先检查 skill
- "我得先收集更多上下文" → skill 检查必须**先于**澄清问题
- "让我先探索一下代码库" → skill 会告诉你怎么探索
- "这事不需要正式 skill" → 如果 skill 存在，就用它
- "用 skill 太重了" → 简单的事情会变复杂
- "我知道那是什么意思" → 知道概念 ≠ 使用 skill
- "我先做完这一件事再说" → 在做任何事之前先检查

## 子代理例外

如果你是被作为**子代理**派来执行一个具体任务（已经从主代理拿到明确指令），
跳过这个 rule，直接执行交给你的任务。
